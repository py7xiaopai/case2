# A 股行情数据平台 — 产品介绍

> 沪深京全量 A 股历史行情数据的自动化采集、存储、计算与可视化展示平台。
> 提供 RESTful API + 交互式看板两种访问方式，支持 Docker 一键部署。

---

## 核心能力

### 📡 全市场数据覆盖

| 数据类别 | 覆盖范围 |
|---------|---------|
| 股票总数 | **5,516 只**（上海主板+科创板 / 深圳主板+创业板 / 北交所） |
| 日线行情 | 前复权历史数据，含开盘/收盘/最高/最低/成交量/成交额/涨跌幅 |
| 技术指标 | MA5/10/20/30/60、MACD（DIF/DEA/柱）、RSI（6/12/24）、布林带 |
| 交易日历 | 2000–2030 年，支持交易日/非交易日标记 |

### 🔌 RESTful API

基于 FastAPI 构建，附带 Swagger 交互式文档（`/docs`），开箱即用：

| 接口 | 用途 |
|------|------|
| `GET /stocks/search` | 按代码或名称模糊搜索 |
| `GET /stocks/{code}` | 股票详情（含市值、板块、行业） |
| `GET /stocks/{code}/prices` | 历史行情（分页、日期筛选） |
| `GET /stocks/{code}/chart` | K 线图数据序列 |
| `GET /stocks/{code}/indicators` | 技术指标 |
| `GET /stocks/{code}/export` | 导出行情 CSV |
| `GET /rankings/changes` | 涨跌幅排行榜 |
| `GET /rankings/volume` | 成交量排行榜 |
| `GET /quality` | 数据质量报告 |

### 📈 可视化看板

基于 Streamlit 的交互式面板，浏览器访问即可使用：

- **K 线图** — 含 MA5/MA10/MA20 均线叠加，可缩放拖拽
- **成交量图** — 涨红跌绿，与 K 线颜色联动
- **MACD 指标图** — DIF/DEA 线 + 红绿柱
- **近期行情表** — 千分位数字、涨跌颜色标识、最新交易日高亮
- **股票详情卡** — 板块、行业、市值自动换算（元→亿/万亿）
- **数据概览** — 股票总数、行情条数、更新时间的实时统计

### ⏰ 自动化运维

- **每日 18:00** 自动增量更新行情数据
- **每周日** 自动刷新股票列表
- **每日** 检查缺失字段并自动回填
- 全流程日志记录，异常可追溯

---

## 技术架构

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   看板层      │    │   服务层      │    │   调度层      │
│  Streamlit   │    │   FastAPI    │    │  APScheduler  │
│  交互图表     │    │   REST接口    │    │  定时更新      │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │
                    ┌──────▼───────┐
                    │   数据库      │
                    │   MySQL 8.0   │
                    │  6 张核心表   │
                    └──────────────┘
```

### 数据源

来自新浪财经网页接口，模拟浏览器访问，稳定可靠。

---

## 部署方式

### Docker 一键部署（推荐）

```bash
docker compose up -d --build
```

启动后访问：

| 服务 | 地址 |
|------|------|
| API 交互文档 | http://localhost:8000/docs |
| 可视化看板 | http://localhost:8501 |

### 环境要求

- Docker & Docker Compose
- 或：Python 3.10+ / MySQL 8.0（本地开发）

---

## 应用场景

- **量化投研** — 通过 API 批量获取行情数据和技术指标
- **投资分析** — 看板可视化浏览个股 K 线、MACD、涨跌幅排行
- **数据服务** — 为内部系统提供标准化的 A 股行情数据接口
- **策略回测** — 导出 CSV 历史数据进行策略验证

---

## 后续可扩展方向

- 财务数据接入（PE/PB、营收、ROE）
- 多股对比看板
- 选股策略模型（多因子 / 技术形态识别）
- 异动股通知告警（钉钉/微信）
- 分钟级实时行情

---

> **版本**: MVP v0.1.0 · 最后更新: 2026-05-16
